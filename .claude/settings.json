{
  "$comment": "Example wiring for Habemus Deploy. Copy this PreToolUse block into your own .claude/settings.json (project) or ~/.claude/settings.json (all projects). Adjust the command path to where you installed bless-deploy.sh. The hook is non-blocking: it never denies, asks, or auto-approves a deploy.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/hooks/bless-deploy.sh"
          }
        ]
      }
    ]
  }
}
